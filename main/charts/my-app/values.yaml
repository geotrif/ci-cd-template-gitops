replicaCount: 2

image:
  repository: ghcr.io/geotrif/ci-cd-template
  tag: "sha-5f8b3d6"        # default; overridden by Image Updater OR manually
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources: {}

ingress:
  enabled: false

autoscaling:
  enabled: false
